server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — ทุก route ที่ไม่ใช่ไฟล์จริง ให้ตกมาที่ index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
