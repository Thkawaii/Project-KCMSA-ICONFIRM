# URL ของ backend Go API — ต้องตรงกับชื่อตัวแปรที่ src/api/client.js อ่าน (VITE_API_BASE_URL)
# ค่า default ถ้าไม่ตั้ง = http://localhost:8080 (ตรงกับพอร์ต default ของ backend)
# หมายเหตุ: backend ไม่มี prefix /api ดังนั้น "ห้าม" ใส่ /api ต่อท้าย
VITE_API_BASE_URL=http://localhost:8080
